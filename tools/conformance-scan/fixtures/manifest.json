{
  "_comment": [
    "What `diff.ts --selftest` must report for actual-dirty.json — EXACTLY this set, no more and no less.",
    "",
    "Hand-written, and that is the whole point: a manifest generated from the diff's own output would be a",
    "gate built from its subject (docs/34-gate-independence.md) and could not fail. Each row is",
    "`<category>: <subject>` as the report prints it, so a row that stops appearing names the arm that",
    "broke. The self-check also asserts every one of the nine categories appears here at least once, so a",
    "row silently lost from this file fails as missing COVERAGE rather than passing as a smaller set.",
    "",
    "Adding a defect to actual-dirty.json without adding its row here fails as an EXTRA. That is intended:",
    "the fixture and this file move together or the self-check says so.",
    "",
    "A row is `<category>: <subject>` and therefore a SET, not a count: where two findings land on one",
    "subject — `caption/md/default` is both a raw value where a variable was planned and the wrong weight —",
    "they collapse to a single row, and an arm that lost one of the two would still find the row here. That",
    "gap is closed in mutations.sh, which asserts those findings by their own wording; this file is the",
    "coarse net and that one is the fine one."
  ],
  "clean": "actual-clean.json",
  "dirty": "actual-dirty.json",
  "expect": [
    "binding-presence: widget · size=md, state=rest · / · fills",
    "binding-presence: gadget · size=sm · / · fills",
    "binding-presence: widget · size=md, state=rest · /label · textStyle",
    "binding-presence: widget · size=md, state=rest · /caption · textStyle",
    "binding-presence: widget · size=md, state=rest · /caption · fills",
    "binding-target: widget · size=md, state=rest · /label · fills",
    "binding-target: widget · size=md, state=hover · / · strokeWeight",
    "value-match: fix/core/palette/neutral/900 [Default]",
    "mode-coverage: fix/color/icon/primary [dark]",
    "scope-type: fix/opacity/veil",
    "scope-type: fix/size/md/height",
    "structure: collection 'legacy'",
    "structure: component 'gadget'",
    "structure: fix/color/text/secondary",
    "style-definition: text style 'body/md/default'",
    "style-definition: text style 'caption/md/default'",
    "style-definition: text style 'label/sm/default'",
    "style-definition: effect style 'shadow/sm'",
    "style-definition: grid style 'Grid / xs'",
    "style-definition: paint style 'gradient/brand'",
    "style-definition: text style 'designer/scratch'",
    "contrast: light · text.primary on background.primary",
    "contrast: light · interactive.overlay.hover on background.primary",
    "staleness: engine version"
  ]
}
